[
  {
    "id": 13,
    "author": "user",
    "payload": {
      "type": "text",
      "text": "Altere somente o solicitado abaixo:\n1- quando um free text (1) entra em uma categoria e tem outro free text (2) abaixo dele, no momento que entra o free text (2) abaixo dele sobe uma linha de posição, isso nao pode ocorrer",
      "files": [],
      "fileUploads": [],
      "cMarkNode": {}
    },
    "createdTimestamp": {
      "seconds": 1769269048,
      "nanos": 10000000
    }
  },
  {
    "id": 14,
    "author": "model",
    "payload": {
      "type": "thinking",
      "text": "**Analyzing Drag-and-Drop Bug**\n\nI've identified a glitch in the drag-and-drop mechanics for \"Free Text\" items within categories. It seems that when a \"Free Text\" item is introduced to a category, a second \"Free Text\" item positioned beneath it unexpectedly shifts upwards. I am now exploring potential causes in the movement algorithms.\n\n\n**Investigating Margin Adjustments**\n\nI'm now zeroing in on the margin calculation or insertion logic, specifically within `useKeyboardMovement.ts`. The behavior strongly suggests a flaw in how margins are adjusted when a \"Free Text\" item enters a category and another \"Free Text\" item sits below. Line 85's \"MOVE UP\" logic, which handles swapping or category insertion, warrants careful analysis, and line 258's \"MOVE DOWN\". The user's description pinpoints a failure in `customMarginTop` computation or compensation when insertion happens.\n\n\n**Examining Move Logic**\n\nI'm now diving into the `useKeyboardMovement.ts` file, particularly the \"MOVE UP\" and \"MOVE DOWN\" sections. I'm focusing on line 134 and 288, which seem to handle category entry and neighbor interactions, and if the user is dragging. I'm checking the scenarios: \"Free Text 1\" enters a category and then, \"Free Text 2\" is below. The prompt's \"sobe uma linha de posição\" strongly suggests `customMarginTop` is being manipulated in this logic, and line 158's neighbor compensation.\n\n\n**Clarifying Item Positioning**\n\nI'm now clarifying the item placement scenarios. The key scenario is \"Free Text 1\" entering a category and having \"Free Text 2\" below. This suggests Free Text 1 is positioned *above* Free Text 2. Considering keyboard movement, the `MOVE UP` logic in `useKeyboardMovement.ts` now seems like the place to start, specifically focusing on how margins and the `targetCat` are assigned. I am also investigating the `compensatableNeighbor` assignment and logic.\n\n\n**Revising Margin Transfer**\n\nI'm now focusing on the margin transfer logic in `useKeyboardMovement.ts`, specifically when a \"Free Text\" item enters a category. My analysis indicates that the `compensatableNeighbor` assignment is the key, and is responsible for affecting the neighbor below. The logic I've analyzed seems to incorrectly push the neighbor item *down* rather than *up*. I'm thinking about edge cases where `currentMargin` could be zero or small, which is important to consider. I'll be looking into the `MOVE DOWN` logic as well, particularly how category headers are involved.\n\n\n**Refining Insertion Analysis**\n\nI'm now revising my thinking on the insertion process. The key scenario is \"Free Text 1\" entering a category *above* \"Free Text 2\". I am focusing on lines 134, 158 and 288 in `useKeyboardMovement.ts`. I am considering how `customMarginTop` (essentially `marginTop`) influences the positioning, and thinking about the margin of the neighbor items in the category below. Specifically, how Free Text 2's previous position is affected after Free Text 1 enters the category.\n\n\n**Revisiting Margin Swaps**\n\nI'm now revising my understanding. Previously, I was focused on Free Text 2's movement, but the latest information points to a margin swap or manipulation problem. Specifically, the \"swap\" logic on lines 105 in `Try A` and also `Try B`'s movement code, is now my key focus. Free Text 1 seems to \"enter\" category A, and I'm reconsidering how the margins are handled in those insertion scenarios.\n\n\n**Pinpointing Drag Logic**\n\nI am now focusing on the drag-and-drop mechanism within `useDraggableInteractions.ts`. Specifically, I'm examining the scenario where a Free Text item is dragged over another Free Text item. The `handleDragOverItem` function is my primary focus, particularly the reordering logic when dealing with product types and category differences.\n\n\n**Evaluating Margin Effects**\n\nI'm now focusing on what happens when Free Text 1 (FT1) is dragged into the category of Free Text 2 (FT2). Specifically, FT2's `customMarginTop` is *not* being updated, yet the user reports it \"moves up.\" This means FT1's insertion seems to *remove* an assumed spacing, which implies a miscalculation or incorrect margin usage. I'm focusing on the product insertion process now.\n\n\n**Refining Height Compensation**\n\nI am now focusing on compensating for the height of Free Text 1 when it leaves its position above Free Text 2. The key is to add FT1's height (plus margin) to FT2's margin, or at least, add FT1's margin to FT2's original margin, to maintain visual positioning. This logic appears within `useKeyboardMovement.ts`.\n\n\n**Correcting Height Compensation**\n\nI now understand the issue: the margin compensation in `useKeyboardMovement.ts` only addresses the margin of the item being moved, not its *height*. As a result, when Free Text 1 enters a category above Free Text 2, Free Text 2 \"jumps up\" by the height of Free Text 1, not just its margin. I'm modifying the compensation logic.\n\n\n**Fixing Snowplow Effect**\n\nMy current focus is on the \"Snowplow Fix\" within `useKeyboardMovement.ts`. I've realized the existing logic only considers margin transfers, neglecting element height. When FT1 moves, we must add an estimate of its height to FT2's margin to keep its absolute position. It appears `calculateItemHeight` isn't imported, but estimating the gap (e.g. ~3px) should maintain visual integrity, particularly when entering a category.\n\n\n**Adjusting Category Movement**\n\nI'm now integrating height compensation into `useKeyboardMovement.ts` to address the \"moves up one line\" issue when moving *into* a category. I've pinpointed lines 158 and 189 in \"Move UP\", and am updating the margin calculation to include an `ESTIMATED_HEIGHT` value. I'm also considering cases where an item moves down into a category, requiring a method to track \"previous neighbors.\"\n\n\n**Clarifying Downward Movement**\n\nI'm now clarifying the \"moves down\" scenario within `useKeyboardMovement.ts`. I think the problem happens during a *downward* keyboard movement, when the item *enters* a category. It feels that the item does a swap with the neighbor. This differs from my previous scenarios. The key is to understand when the item to be \"swapped\" with is a header item. I am still focusing on the scenarios when an item drags down into a category.\n\n\n\n**Verifying Movement Logic**\n\nI'm now focusing on validating the code within `useKeyboardMovement.ts`, specifically how it handles margin adjustments when an item enters a category. My current thinking points to a height compensation omission in the `Try B` / `Try C` logic. The reflow with `updateLiveProductOrder` in `useDraggableInteractions.ts`, and the user's assumption of absolute positioning with \"Free Text\" items, have clarified the approach. I am looking at how `customMarginTop: 0` interacts with height changes.\n\n\n**Refining Height Updates**\n\nI'm now integrating estimated height into the margin adjustments, especially around line 163 in `useKeyboardMovement.ts`. My initial thought was a simple constant (around 24 or 30px) to represent the height, but I'll now be using `calculateItemHeight` from `utils/menuPagination.ts` to get a more accurate height, accounting for font size, and add that to `nextMargin`. The goal is to accurately calculate the visual position of items.\n\n\n\n**Implementing Height Compensation**\n\nI am now implementing the height compensation within `useKeyboardMovement.ts`. I've realized adding `COMPENSATION_HEIGHT` (e.g., 40px) to the existing margin transfer logic aligns with the observed behavior. I must apply this height adjustment during item insertion, specifically within `Try B` (lines 134-177) and `Try C` (lines 179-224), to accurately reflect the visual displacement and spacing.\n\n\n**Prioritizing Keyboard Compensation**\n\nI am now focusing on keyboard-driven item movement. Drag-and-drop handles immediate placement, but \"sobe uma linha\" indicates an unintended shift after an action, likely via keyboard. I have identified that `handleDragOverItem` in `useDraggableInteractions.ts` does not dynamically calculate margin compensations during the drag. I'm prioritizing `useKeyboardMovement.ts` for margin adjustments.\n\n\n\n**Implementing Height Compensation**\n\nI'm now integrating the visual block height compensation, focusing on adding `VISUAL_BLOCK_HEIGHT` (40px, or 1 line + padding) to the margin adjustment within the `useKeyboardMovement.ts`. I will leverage the product `id` and inspect if the item is a Free Text, which I can get from `products`. If it is a Free Text item, I will apply a height offset to account for visual displacement during category entry and exit, adding to the margin value during the compensation process in the move logic blocks.\n\n\n\n**Refining Visual Displacement**\n\nI'm now refining the visual block compensation. I am checking the height of the item: If `product.isFreeText` evaluates to true, I am applying a margin adjustment. Instead of a fixed value, I am using a robust constant, such as `LINE_HEIGHT_COMPENSATION = 35`, which I will add to the margin calculation. I am focusing on the `value` assignment within the existing `useKeyboardMovement.ts` code, replacing `nextMargin + currentMargin` with `nextMargin + currentMargin + (product.isFreeText ? 35 : 0)`.\n\n\n**Prioritizing Keyboard Navigation**\n\nI'm now zeroing in on keyboard movement, as the \"jump\" is very jarring for users. The drag-and-drop code in `useDraggableInteractions` commits the order, but doesn't handle the visual compensation needed. It has become clear the keyboard nudging feature, not the drag-and-drop mechanism, is the cause for the \"jump\". I will be focusing on fixing the logic in `useKeyboardMovement.ts` to manage these scenarios.\n\n\n**Adjusting Keyboard Behavior**\n\nI'm now focusing on the keyboard movement and refining the visual displacement compensation. Previously, the code only accounted for margin transfers. Now, I'm integrating `LINE_HEIGHT_COMPENSATION` (40px) into the margin calculations in `useKeyboardMovement.ts`. This involves adding the compensation value when handling scenarios of category entry or exit, specifically when FT1 moves *up* into a category, so it does not jump over header.\n\n\n**Refining Merge-Up Behavior**\n\nI'm now revising the merge-up behavior in `useKeyboardMovement.ts`, focusing on height compensation during category merges. I've updated the `compensatableNeighbor` assignment with the estimated item height to prevent visual \"jumps\". I am updating the `performMergeUp` function to handle product category merges, making sure that `customMarginTop` is handled, plus the visual adjustment for height with the `getCompensatableNeighborBelow` function.\n\n```javascript\n// Inside Try B (Line ~163)\nconst compensatableNeighbor = getCompensatableNeighborBelow();\nif (compensatableNeighbor && compensatableNeighbor.product) {\n    const nextMargin = Number(compensatableNeighbor.product.customMarginTop || 0);\n    // FIX: Add height compensation\n    const heightComp = product.isFreeText ? LINE_HEIGHT_COMPENSATION : 0;\n    updates.push({\n        id: compensatableNeighbor.id,\n        field: 'customMarginTop',\n        value: nextMargin + currentMargin + heightComp // Transfer margin + height\n    });\n}\n\n// Inside Try C (Line ~200) - Case 1 & Case 2 use performMergeUp\n// The performMergeUp function definition needs update\nconst performMergeUp = (targetCat: string) => {\n     // ...\n     // COMPENSATION FIX: Pass margin to neighbor below\n     const compensatableNeighbor = getCompensatableNeighborBelow();\n     if (compensatableNeighbor && compensatableNeighbor.product) {\n         const nextMargin = Number(compensatableNeighbor.product.customMarginTop || 0);\n         // FIX: Add height compensation\n```\n\n\n**Refining Compensation Calculations**\n\nI'm now refining the compensation logic within `useKeyboardMovement.ts`. My key focus is accurate height calculations for Free Text items. I will use a more accurate estimated height, accounting for font size and padding. I will replace the constant `LINE_HEIGHT_COMPENSATION` with a dynamic value calculated from font size and spacing, and ensure that it compensates for the visual displacement when an item enters or leaves a category. The merge-up cases are also considered.\n\n\n**Validating Height Compensation**\n\nI am now focusing on keyboard navigation and validating my fix. The height compensation logic in `useKeyboardMovement.ts` now incorporates a dynamic height calculation to prevent Free Text items from visually jumping when moving into or out of categories. I'm focusing on the refactored code block, specifically the new height computation and its integration with existing margin calculations.\n\n\n**Validating Height Compensation**\n\nI'm now validating the keyboard navigation fix, and how height compensation in `useKeyboardMovement.ts` now handles dynamic calculations to stop visual jumping. I am focusing on reviewing the refactored code block, specifically the new height computation and its integration with the margin calculations. The logic now considers the `getCompensatableNeighborBelow` function.\n\n\n**Implementing Dynamic Height**\n\nI've refactored the height calculation, incorporating dynamic height. The new logic uses `product.styles?.fontSize` with a fallback and applies appropriate padding for accurate margin compensation. I've also refactored the helper function, making my fix more robust. I am validating now, particularly with the `getCompensatableNeighborBelow` function.\n\n\n**Refining Height Compensation**\n\nI'm now refining height compensation in keyboard movement with a focus on free text visual displacement. I'm using `product.styles?.fontSize` to dynamically compute visual height, along with padding, for `compensatableNeighbor`. This ensures accurate margin adjustments and prevents the \"jump\" when entering or exiting categories. I'm now validating the logic.\n\n\n",
      "cMarkNode": {}
    },
    "createdTimestamp": {
      "seconds": 1769269048,
      "nanos": 10000000
    },
    "doneTimestamp": {
      "seconds": 1769269145,
      "nanos": 108000000
    }
  },
  {
    "id": 15,
    "author": "model",
    "payload": {
      "type": "generationTable",
      "entries": [
        {
          "path": "hooks/interactions/useKeyboardMovement.ts",
          "generationType": 1,
          "isDone": true,
          "description": "Fix visual jump when Free Text item moves up into a category by compensating for its height in the margin of the item below it.",
          "diffs": [
            {
              "target": "",
              "replacement": "import React, { useRef, useMemo } from 'react';\nimport { Product } from '../../types';\nimport { InteractionProps, DraftItem, NUDGE_STEP, FREE_TEXT_PREFIX, STANDARD_GAP, A4_HEIGHT_PX } from './types';\n\nexport const useKeyboardMovement = (\n    props: InteractionProps,\n    sortedCategories: string[],\n    groupedProducts: Record<string, Product[]>,\n    draftItem: DraftItem | null,\n    setDraftItem: (item: DraftItem | null) => void\n) => {\n    const { \n        products, scale,\n        onMoveCategory, onMoveProduct, onUpdateProduct, onUpdateProducts,\n        onCommitProductOrder, onStyleUpdate, onAddProduct, onAddCategory, onToggleProductVisibility, onDeleteProduct\n    } = props;\n\n    const draftInputRef = useRef<HTMLDivElement>(null);\n\n    // 3. Flattened Visual List (For Arrow Navigation)\n    const visualList = useMemo(() => {\n        const list: { type: 'header' | 'product', id: string, category: string, product?: Product, globalIndex: number }[] = [];\n        let gIdx = 0;\n        sortedCategories.forEach(cat => {\n            list.push({type: 'header',id: cat,category: cat,globalIndex: gIdx++});\n            const prods = groupedProducts[cat] || [];\n            prods.forEach(p => {\n                list.push({ type: 'product', id: p.id, category: cat, product: p, globalIndex: gIdx++ });\n            });\n        });\n        return list;\n    }, [sortedCategories, groupedProducts]);\n\n    const handleGlobalMove = (e: React.MouseEvent, type: 'category' | 'product', id: string, catName: string, direction: 'up' | 'down') => {\n        e.stopPropagation();\n        \n        // Standard Category Move\n        if (type === 'category') {\n            onMoveCategory?.(id, direction);\n            return;\n        }\n        \n        const product = products.find(p => p.id === id);\n        if (!product) return;\n\n        if (!product.isFreeText) {\n            onMoveProduct?.(id, catName, direction);\n            return;\n        }\n        \n        // --- UNIFIED FREE TEXT MOTION LOGIC ---\n        const currentIndex = visualList.findIndex(item => item.id === id);\n        if (currentIndex === -1) return;\n        \n        const neighbor = direction === 'up' ? visualList[currentIndex - 1] : visualList[currentIndex + 1];\n        \n        // Helper to estimate height for compensation\n        const getHeightCompensation = (p: Product) => {\n            if (!p.isFreeText) return 0;\n            const fontSize = p.styles?.fontSize || 18;\n            return (fontSize * 1.5) + 15; // Estimated visual height (1.5 line height + 15 padding)\n        };\n\n        // --- MOVE UP (FAIL-SAFE FLOW) ---\n        if (direction === 'up') {\n            // 1. ROBUST DATA FETCHING\n            const currentMargin = Number(product.customMarginTop || 0);\n            const neighborAbove = visualList[currentIndex - 1]; \n            const neighborBelow = visualList[currentIndex + 1];\n\n            // Helper to get the actual item below to compensate (skipping its header if needed)\n            const getCompensatableNeighborBelow = () => {\n                let target = neighborBelow;\n                if (target && target.type === 'header') {\n                    // If neighbor below is a header, look at the first product inside it\n                    target = visualList[currentIndex + 2];\n                }\n                return (target && target.type === 'product' && target.product) ? target : null;\n            };\n\n            // 2. INTERACTION PHASE (Only if touching neighbor above)\n            if (neighborAbove && currentMargin <= STANDARD_GAP) {\n                \n                // Try A: SWAP (Free Text or Same Category)\n                if (neighborAbove.type === 'product' && neighborAbove.product &&\n                    (neighborAbove.product.isFreeText || neighborAbove.product.category === product.category)) {\n                    \n                    if (onUpdateProducts && onCommitProductOrder) {\n                        const neighborMargin = Number(neighborAbove.product.customMarginTop ?? 0);\n                        const updates: { id: string, field: keyof Product, value: any }[] = [\n                            { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                            { id: neighborAbove.id, field: 'customMarginTop' as keyof Product, value: currentMargin },\n                        ];\n                        if (product.category !== neighborAbove.product.category) {\n                            updates.push({ id: product.id, field: 'category' as keyof Product, value: neighborAbove.product.category });\n                            updates.push({ id: neighborAbove.id, field: 'category' as keyof Product, value: product.category });\n                        }\n                        onUpdateProducts(updates);\n\n                        if (product.category === neighborAbove.product.category) {\n                            const currentOrder = groupedProducts[product.category].map(p => p.id);\n                            const idx1 = currentOrder.indexOf(id);\n                            const idx2 = currentOrder.indexOf(neighborAbove.id);\n                            if (idx1 !== -1 && idx2 !== -1) {\n                                const newOrder = [...currentOrder];\n                                [newOrder[idx1], newOrder[idx2]] = [newOrder[idx2], newOrder[idx1]];\n                                onCommitProductOrder(product.category, newOrder);\n                            }\n                        }\n                    }\n                    return; // Success\n                }\n\n                // Try B: ENTER CATEGORY (Bottom-Up Entry - Direct Neighbor Product)\n                if (neighborAbove.type === 'product' && neighborAbove.product && \n                    !neighborAbove.product.category.startsWith(FREE_TEXT_PREFIX)) {\n                        \n                        const targetCat = neighborAbove.product.category;\n                        if (onUpdateProducts && onCommitProductOrder) {\n                            const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                            const neighborIndex = currentOrder.indexOf(neighborAbove.id);\n                            const newOrder = [...currentOrder];\n                            \n                            // Append after neighbor (index + 1)\n                            if (neighborIndex !== -1) newOrder.splice(neighborIndex + 1, 0, product.id);\n                            else newOrder.push(product.id);\n\n                            const updates: { id: string, field: keyof Product, value: any }[] = [\n                                { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                                { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                            ];\n\n                            // COMPENSATION FIX: Pass margin + height to neighbor below\n                            const compensatableNeighbor = getCompensatableNeighborBelow();\n                            if (compensatableNeighbor && compensatableNeighbor.product) {\n                                const nextMargin = Number(compensatableNeighbor.product.customMarginTop || 0);\n                                const heightComp = getHeightCompensation(product);\n                                updates.push({\n                                    id: compensatableNeighbor.id,\n                                    field: 'customMarginTop',\n                                    value: nextMargin + currentMargin + heightComp // Transfer margin + height\n                                });\n                            }\n\n                            onUpdateProducts(updates);\n                            onCommitProductOrder(targetCat, newOrder);\n                        }\n                        return; // Success\n                }\n                \n                // Try C: HEADER EXIT\n                if (neighborAbove.type === 'header') {\n                        // MERGE INTO CATEGORY ABOVE logic\n                        const itemAboveHeader = visualList[currentIndex - 2];\n\n                        // Common Logic for merging up\n                        const performMergeUp = (targetCat: string) => {\n                             if (onUpdateProducts && onCommitProductOrder) {\n                                 const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                                 const newOrder = currentOrder.includes(product.id) ? currentOrder : [...currentOrder, product.id];\n                                 \n                                 const updates: { id: string, field: keyof Product, value: any }[] = [\n                                     { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                                     { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                                 ];\n\n                                 // COMPENSATION FIX: Pass margin + height to neighbor below\n                                 const compensatableNeighbor = getCompensatableNeighborBelow();\n                                 if (compensatableNeighbor && compensatableNeighbor.product) {\n                                     const nextMargin = Number(compensatableNeighbor.product.customMarginTop || 0);\n                                     const heightComp = getHeightCompensation(product);\n                                     updates.push({\n                                         id: compensatableNeighbor.id,\n                                         field: 'customMarginTop',\n                                         value: nextMargin + currentMargin + heightComp // Transfer margin + height\n                                     });\n                                 }\n\n                                 onUpdateProducts(updates);\n                                 onCommitProductOrder(targetCat, newOrder);\n                            }\n                        };\n\n                        // Case 1: Above header is a product in a regular category\n                        if (itemAboveHeader && itemAboveHeader.type === 'product' && \n                            !itemAboveHeader.category.startsWith(FREE_TEXT_PREFIX)) {\n                                performMergeUp(itemAboveHeader.category);\n                                return;\n                        }\n\n                        // Case 2: Above header is a header of a regular category (empty)\n                        if (itemAboveHeader && itemAboveHeader.type === 'header' && \n                            !itemAboveHeader.category.startsWith(FREE_TEXT_PREFIX)) {\n                                performMergeUp(itemAboveHeader.category);\n                                return;\n                        }\n\n                        // Fallback: Create new ghost category\n                        if (onStyleUpdate && onUpdateProduct) {\n                            const newGhostCat = `${FREE_TEXT_PREFIX}${crypto.randomUUID()}`;\n                            onStyleUpdate(prev => {\n                                const cats = [...(prev.customCategoryOrder || sortedCategories)];\n                                const idx = cats.indexOf(neighborAbove.category);\n                                if (idx !== -1) cats.splice(idx, 0, newGhostCat);\n                                return { ...prev, customCategoryOrder: cats, name: 'Custom' };\n                            });\n                            onUpdateProduct(id, 'category', newGhostCat);\n                        }\n                        return; // Success\n                }\n\n                // Try D: FORCE SWAP (Fallback for Blocking)\n                // If we reached here, we are touching a product but couldn't enter. \n                // We MUST jump over it to prevent getting stuck.\n                if (neighborAbove.type === 'product' && neighborAbove.product) {\n                        // Treat as swap even if different category (simplistic fallback)\n                        if (onUpdateProducts) {\n                            const neighborMargin = Number(neighborAbove.product.customMarginTop ?? 0);\n                            onUpdateProducts([\n                            { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                            { id: neighborAbove.id, field: 'customMarginTop' as keyof Product, value: currentMargin },\n                            // Swap categories\n                            { id: product.id, field: 'category' as keyof Product, value: neighborAbove.product.category },\n                            { id: neighborAbove.id, field: 'category' as keyof Product, value: product.category }\n                        ]);\n                        }\n                        return;\n                }\n            }\n\n            // 3. MOVEMENT PHASE (Nudge Up)\n            // If we didn't interact (or had space), we move up.\n            \n            const newMyMargin = Math.max(0, currentMargin - NUDGE_STEP);\n            const updates: { id: string, field: keyof Product, value: any }[] = [\n                { id: product.id, field: 'customMarginTop', value: newMyMargin }\n            ];\n\n            // B. COMPENSATE NEIGHBOR BELOW (The Snowplow Fix)\n            // CRITICAL: This MUST run if neighborBelow exists, regardless of anything else.\n            \n            let neighborToCompensate = neighborBelow;\n\n            // Fix for Free Text / Cross-Category: \n            // If the immediate neighbor is a header, we check the item AFTER the header.\n            // If that item is a product, we apply compensation to it instead.\n            if (neighborToCompensate && neighborToCompensate.type === 'header') {\n                const nextVisual = visualList[currentIndex + 2];\n                if (nextVisual && nextVisual.type === 'product') {\n                    neighborToCompensate = nextVisual;\n                }\n            }\n\n            if (neighborToCompensate && neighborToCompensate.type === 'product' && neighborToCompensate.product) {\n                const nextMarginRaw = neighborToCompensate.product.customMarginTop;\n                // Force Number() to prevent NaN\n                const nextMarginSafe = Number(nextMarginRaw ?? 0); \n                \n                // We add EXACTLY the same amount we subtracted from the item above (Conservation of Space)\n                updates.push({ \n                    id: neighborToCompensate.id, \n                    field: 'customMarginTop', \n                    value: nextMarginSafe + NUDGE_STEP \n                });\n            }\n\n            onUpdateProducts?.(updates);\n        }\n\n        // --- MOVE DOWN ---\n        if (direction === 'down') {\n            // Bottom Exit (Leaving Category Downwards)\n            if ((!neighbor || neighbor.type === 'header') && !catName.startsWith(FREE_TEXT_PREFIX)) {\n                if (onStyleUpdate && onUpdateProducts) {\n                    const newGhostCat = `${FREE_TEXT_PREFIX}${crypto.randomUUID()}`;\n                    onStyleUpdate(prev => {\n                        const cats = [...(prev.customCategoryOrder || sortedCategories)];\n                        const idx = cats.indexOf(catName);\n                        if (idx !== -1) cats.splice(idx + 1, 0, newGhostCat); // Insert AFTER\n                        else cats.push(newGhostCat);\n                        return { ...prev, customCategoryOrder: cats, name: 'Custom' };\n                    });\n                    onUpdateProducts([\n                        { id: product.id, field: 'category' as keyof Product, value: newGhostCat },\n                        { id: product.id, field: 'customMarginTop' as keyof Product, value: STANDARD_GAP }\n                    ]);\n                }\n                return;\n            }\n\n            // Allow moving down (increasing margin) even if no neighbor in a ghost zone\n            if (!neighbor && catName.startsWith(FREE_TEXT_PREFIX)) {\n                    const myMargin = product.customMarginTop || 0;\n                    onUpdateProduct?.(id, 'customMarginTop', myMargin + NUDGE_STEP);\n                    return;\n            }\n\n            if (!neighbor) return;\n\n            // 1. Physics Check (Nudge Down)\n            if (neighbor.type === 'product' && neighbor.product) {\n                const neighborMargin = neighbor.product.customMarginTop || 0;\n\n                // If neighbor has margin, consume it (Zero-Sum)\n                if (neighborMargin > STANDARD_GAP) {\n                    const myMargin = product.customMarginTop || 0;\n                    onUpdateProducts?.([\n                        { id: product.id, field: 'customMarginTop' as keyof Product, value: myMargin + NUDGE_STEP },\n                        { id: neighbor.id, field: 'customMarginTop' as keyof Product, value: neighborMargin - NUDGE_STEP }\n                    ]);\n                    return;\n                } else if (catName.startsWith(FREE_TEXT_PREFIX)) {\n                        // Fallthrough to collision\n                }\n            }\n\n            // --- COLLISION LOGIC ---\n\n            // FT vs FT Collision (Down)\n            if (neighbor.type === 'product' && neighbor.product?.isFreeText) {\n                    if (onUpdateProducts) {\n                        const myMargin = product.customMarginTop || 0;\n                        const neighborMargin = neighbor.product.customMarginTop || 0;\n                        onUpdateProducts([\n                            { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                            { id: neighbor.id, field: 'customMarginTop' as keyof Product, value: myMargin },\n                            { id: product.id, field: 'category' as keyof Product, value: neighbor.category },\n                            { id: neighbor.id, field: 'category' as keyof Product, value: catName }\n                        ]);\n                        if (neighbor.category === catName && onCommitProductOrder) {\n                        const currentOrder = groupedProducts[catName].map(p => p.id);\n                        const idx1 = currentOrder.indexOf(id);\n                        const idx2 = currentOrder.indexOf(neighbor.id);\n                        const newOrder = [...currentOrder];\n                        [newOrder[idx1], newOrder[idx2]] = [newOrder[idx2], newOrder[idx1]];\n                        onCommitProductOrder(catName, newOrder);\n                    }\n                    }\n                    return;\n            }\n            \n            // Case A: Neighbor is Header (Next Category)\n            if (neighbor.type === 'header') {\n                // Enter Category (Top)\n                if (onUpdateProducts && onCommitProductOrder) {\n                    const targetCat = neighbor.id;\n                    onUpdateProducts([\n                        { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                        { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                    ]);\n                    const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                    onCommitProductOrder(targetCat, [product.id, ...currentOrder]);\n                }\n                return;\n            }\n\n            // Case B: Neighbor is Product\n            if (neighbor.type === 'product' && neighbor.product) {\n                    if (neighbor.category === catName) {\n                        if (onCommitProductOrder && onUpdateProducts) {\n                            const currentOrder = groupedProducts[catName].map(p => p.id);\n                            const idx1 = currentOrder.indexOf(id);\n                            const idx2 = currentOrder.indexOf(neighbor.id);\n                            const newOrder = [...currentOrder];\n                            [newOrder[idx1], newOrder[idx2]] = [newOrder[idx2], newOrder[idx1]];\n\n                            const myMargin = product.customMarginTop || 0;\n                            const neighborMargin = neighbor.product.customMarginTop || 0;\n                            onUpdateProducts([\n                                { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                                { id: neighbor.id, field: 'customMarginTop' as keyof Product, value: myMargin }\n                            ]);\n                            onCommitProductOrder(catName, newOrder);\n                        }\n                        return;\n                    }\n                    \n                    // Subcase B2: Different Category\n                    if (neighbor.category !== catName) {\n                        const targetCat = neighbor.category;\n                        if (onUpdateProducts && onCommitProductOrder) {\n                            onUpdateProducts([\n                                { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                                { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                            ]);\n                            // Insert Before Neighbor\n                            const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                            const nIdx = currentOrder.indexOf(neighbor.id);\n                            const newOrder = [...currentOrder];\n                            if (nIdx !== -1) newOrder.splice(nIdx, 0, product.id);\n                            else newOrder.unshift(product.id);\n                            \n                            onCommitProductOrder(targetCat, newOrder);\n                        }\n                        return;\n                    }\n            }\n        }\n    };\n\n    const handlePageDoubleClick = (e: React.MouseEvent, pageIndex: number, handleSelection: any, setSelectedPageIndex: any) => {\n        e.stopPropagation();\n        handleSelection(null, null);\n        \n        const pageEl = e.currentTarget as HTMLElement;\n        const pageRect = pageEl.getBoundingClientRect();\n        const currentScale = (typeof scale === 'number' && scale > 0) ? scale : 1;\n        const clickY = (e.clientY - pageRect.top) / currentScale;\n        \n        let floorId: string | null = null;\n        let floorBottom = 0; \n        let ceilingId: string | null = null;\n        let ceilingTop = A4_HEIGHT_PX; \n        \n        let minDistAbove = Infinity;\n        let minDistBelow = Infinity;\n\n        const allElements = pageEl.querySelectorAll('[id^=\"product-container-\"], [id^=\"category-header-\"], [data-block-id=\"main-header\"]');\n        \n        allElements.forEach(el => {\n            const rect = el.getBoundingClientRect();\n            const itemTop = (rect.top - pageRect.top) / currentScale;\n            const itemHeight = rect.height / currentScale;\n            const itemBottom = itemTop + itemHeight;\n            \n            let id = el.getAttribute('data-category-id') || el.getAttribute('data-block-id');\n            if (!id && el.id.startsWith('product-container-')) id = el.id.replace('product-container-', '');\n            if (!id) return;\n            \n            if (itemBottom < clickY) {\n                const dist = clickY - itemBottom;\n                if (dist < minDistAbove) {\n                    minDistAbove = dist;\n                    floorId = id;\n                    floorBottom = itemBottom;\n                }\n            } else if (itemTop > clickY) {\n                const dist = itemTop - clickY;\n                if (dist < minDistBelow) {\n                    minDistBelow = dist;\n                    ceilingId = id;\n                    ceilingTop = itemTop;\n                }\n            }\n        });\n        \n        setDraftItem({\n            pageIndex,\n            top: clickY,\n            floorId,\n            floorBottom,\n            ceilingId,\n            ceilingTop\n        });\n\n        setTimeout(() => {\n            if (draftInputRef.current) draftInputRef.current.focus();\n        }, 50);\n    };\n\n    const handleDraftCommit = () => {\n        if (!draftItem || !onAddProduct || !onStyleUpdate || !draftInputRef.current) return;\n        const text = draftInputRef.current.innerText || \"New Text\";\n        const newId = crypto.randomUUID();\n        const ghostCategoryName = `${FREE_TEXT_PREFIX}${newId}`;\n        \n        const newItemMargin = Math.max(0, draftItem.top - draftItem.floorBottom);\n        \n        if (draftItem.ceilingId && onUpdateProduct) {\n            const currentGap = draftItem.ceilingTop - draftItem.floorBottom;\n            const ESTIMATED_HEIGHT = 40; \n            const newCeilingMargin = Math.max(0, currentGap - newItemMargin - ESTIMATED_HEIGHT);\n            \n            const isCeilingProduct = products.find(p => p.id === draftItem.ceilingId);\n            if (isCeilingProduct) {\n                onUpdateProduct(draftItem.ceilingId, 'customMarginTop', newCeilingMargin);\n            }\n        }\n        \n        onStyleUpdate(prev => {\n            const currentOrder = prev.customCategoryOrder && prev.customCategoryOrder.length > 0 \n                ? [...prev.customCategoryOrder] \n                : [...sortedCategories];\n            \n            sortedCategories.forEach(c => { if (!currentOrder.includes(c)) currentOrder.push(c); });\n\n            let insertIndex = currentOrder.length;\n            \n            if (draftItem.ceilingId) {\n               const cProd = products.find(p => p.id === draftItem.ceilingId);\n               const cCat = cProd ? cProd.category : (currentOrder.includes(draftItem.ceilingId!) ? draftItem.ceilingId : null);\n               if (cCat) {\n                   const idx = currentOrder.indexOf(cCat);\n                   if (idx !== -1) insertIndex = idx;\n               }\n            } else if (draftItem.floorId) {\n               const fProd = products.find(p => p.id === draftItem.floorId);\n               const fCat = fProd ? fProd.category : (currentOrder.includes(draftItem.floorId!) ? draftItem.floorId : null);\n               if (fCat) {\n                   const idx = currentOrder.indexOf(fCat);\n                   if (idx !== -1) insertIndex = idx + 1;\n               }\n            }\n\n            currentOrder.splice(insertIndex, 0, ghostCategoryName);\n\n            const newProdOrder = { ...(prev.customProductOrder || {}) };\n            newProdOrder[ghostCategoryName] = [newId];\n\n            return {\n                ...prev,\n                customCategoryOrder: currentOrder,\n                customProductOrder: newProdOrder,\n                name: 'Custom'\n            };\n        });\n\n        onAddProduct(ghostCategoryName, undefined, true, newId, { customMarginTop: newItemMargin, name: text });\n        setDraftItem(null);\n    };\n\n    const handleRemove = (e: React.MouseEvent, id: string, type: 'product' | 'category') => {\n        e.stopPropagation();\n        e.preventDefault();\n        \n        if (type === 'product') {\n             const product = products.find(p => p.id === id);\n             if (product?.isFreeText) {\n                 // Free Text: PERMANENT DELETE\n                 onDeleteProduct?.(id);\n             } else {\n                 // Regular Product: HIDE (toggle off)\n                 onToggleProductVisibility?.(id, false);\n             }\n        } else {\n            // Category: HIDE ALL PRODUCTS inside (toggle off)\n            const catProducts = groupedProducts[id] || [];\n            if (onStyleUpdate) {\n                 onStyleUpdate(prev => {\n                     const currentHidden = new Set(prev.hiddenProductIds || []);\n                     catProducts.forEach(p => currentHidden.add(p.id));\n                     return { ...prev, hiddenProductIds: Array.from(currentHidden), name: 'Custom' };\n                 });\n            }\n        }\n    };\n    \n    // Wrapper for add functionality if handlers needs to expose it for MenuItem convenience\n    const handleAddClick = (e: React.MouseEvent, category: string, isCategoryAdd: boolean, position: 'before' | 'after') => {\n        if (isCategoryAdd) {\n            onAddCategory?.(category, position);\n        } else {\n            onAddProduct?.(category, undefined, false, undefined, undefined, { index: 0 }); \n        }\n    };\n\n    return {\n        visualList,\n        draftInputRef,\n        handleGlobalMove,\n        handlePageDoubleClick,\n        handleDraftCommit,\n        handleRemove,\n        handleAddClick\n    };\n};"
            }
          ]
        }
      ]
    },
    "createdTimestamp": {
      "seconds": 1769269145,
      "nanos": 111000000
    },
    "doneTimestamp": {
      "seconds": 1769269197,
      "nanos": 562000000
    }
  },
  {
    "id": 17,
    "author": "user",
    "payload": {
      "type": "text",
      "text": "Altere somente o solicitado abaixo:\n1- agora quando um free text (1) entra em uma categoria e tem outro free text (2) abaixo dele, no momento que entra o free text (2) abaixo dele, ele desce de posição, o mesmo ocorre quando o free text (1) sai da categoria. Isso nao pode ocorrer em nenhum caso, a posição de um free text é absoluta e só deve ser alterada se o usuario clicar para alterar ela. ",
      "files": [],
      "fileUploads": [],
      "cMarkNode": {}
    },
    "createdTimestamp": {
      "seconds": 1769269317,
      "nanos": 404000000
    }
  },
  {
    "id": 18,
    "author": "model",
    "payload": {
      "type": "thinking",
      "text": "**Analyzing Menu Item Behavior**\n\nI'm currently investigating a problematic interaction within the menu application related to \"Free Text\" items and their positioning. It appears that introducing a Free Text item into a category unexpectedly shifts the placement of other Free Text items further down the list. I suspect the recently implemented \"snowplow\" compensation logic is the culprit and am digging into it.\n\n\n**Isolating the Snowplow Bug**\n\nI've determined the issue stems from an interaction between the \"Free Text\" item type and the existing compensation logic, specifically within `handleGlobalMove` in `useKeyboardMovement.ts`. It looks like the \"snowplow\" compensation is triggered when a Free Text item enters or leaves a category. The position of a Free Text item is intended to be \"absolute,\" but this logic causes unintended visual shifts, so I am going to attempt to modify the snowplow logic.\n\n\n**Refining Space Compensation**\n\nI am now focusing on refining the space compensation logic, specifically concerning the interaction of \"Free Text\" items with the `NUDGE_STEP` within the \"snowplow\" compensation. The intent is to conserve space by nudging the item below by `NUDGE_STEP` if the item above is moved. This maintains the visual absolute position of the item below relative to the page. However, Free Text items should not participate in this interaction.\n\n\n**Clarifying Entry Logic**\n\nI'm now zeroing in on the compensation behavior when a Free Text item enters a category. My analysis indicates that the current implementation's reaction to category entry is causing the unwanted positional changes. I'm focusing on the `useKeyboardMovement.ts` file, specifically the \"Bottom-Up Entry\" scenario. The key interaction appears to hinge on how the system handles the relationship between a new Free Text item and any existing items within the category, and if the existing items should move when the new item is added.\n\n\n**Fixing Height Compensation**\n\nI'm now zeroing in on a flaw in the height compensation for the case where a header is exited. It looks like when a header is exited, and the compensatableNeighborBelow() is called, the intention is to transfer the margin and height to the neighbor below. My testing shows the height compensation should not be added to the margin transfer.\n\n\n**Addressing Category Transition**\n\nI'm now investigating the transition of Free Text items into categories, particularly the shift when an item conceptually leaves the \"Free Text Zone.\" My focus is on scenarios where a Free Text item with a large margin enters a category, as this often triggers an unexpected position change for other Free Text items within the category. I am trying to correct margin transfers so the change doesn't happen.\n\n\n**Evaluating Category Shifts**\n\nI'm now focused on transitions when a Free Text item moves *into* a category. If a Free Text item moves into a \"real\" category, the logic is supposed to keep the item below visually stationary, but it's not working. The compensation is likely too much, resulting in an undesired downward visual shift for the subsequent items. I am trying to determine if the issue is with the amount of compensation or if the compensation itself is incorrect for this specific scenario.\n\n\n**Revising Absolute Positioning**\n\nI'm now revisiting the concept of \"absolute position\" for Free Text items, particularly when an item moves into a \"real\" category. The goal is to keep the item's visual position, which is relative to the page, stable. I've re-examined the code, focusing on how category transitions and margin adjustments interact. I'm focusing on why items can have a visual descending shift. I am starting to consider the relationship between standard block flow, explicit margins, and the user's expectation of absolute positioning.\n\n\n**Correcting Downward Shifts**\n\nI'm now investigating why adding height compensation, as currently calculated, causes a downward shift for the subsequent item. My suspicion now centers on an overestimation of the height compensation, or unaccounted-for padding or borders. The intent is to keep the item (2) stationary by adding `currentMargin + heightComp`, but it seems like `heightComp` is too large. I'm focusing on why there is a descending shift, when I intend an absolute, stationary position.\n\n\n**Analyzing Absolute Positioning**\n\nI'm now grappling with the core issue: maintaining absolute positioning for Free Text items during category transitions. It appears the \"descending\" behavior indicates excessive margin adjustments or unnecessary compensation. I'm focusing on why items can have a visual descending shift. I am considering how the interaction of standard block flow, explicit margins, and the user's absolute positioning expectation can lead to errors.\n\n\n**Refining Compensation Adjustments**\n\nI'm now zeroing in on the compensation strategy when a Free Text item enters a category, as that is the source of the descending shift. When FT1 moves into CatA, FT2's visual position is impacted, and I'm correcting for FT2's shift. FT2 is implicitly moved upward by the transfer of FT1's margin. So I am adjusting the margin on FT2, so FT2 appears stationary.\n\n\n**Simplifying Height Compensation**\n\nI now understand the \"descending\" behavior stems from double-counting Free Text item heights during category transitions. If FT1 enters a category above FT2, the category grows, pushing FT2 down. The current compensation unintentionally pushes it down even further. I am simplifying the height compensation by removing the double count, and determining if margin compensation is needed.\n\n\n**Adjusting Free Text Flow**\n\nI'm now focused on the behavior when a Free Text item (FT1) exits a category above another Free Text item (FT2). Initially, FT2 is expected to visually descend to accommodate FT1's space, since this app uses a list layout with margins. But I am adding compensation to move FT2 even further down. It's likely the code is compensating when it shouldn't. I'm focusing on the `compensatableNeighbor` logic in `useKeyboardMovement.ts` to correct this.\n\n\n**Refining Neighbor Logic**\n\nI am now investigating the neighbor logic within the category transition to ensure Free Text items maintain their visual position. In the `ENTER CATEGORY` and `HEADER EXIT` cases, the `compensatableNeighbor` function seems to be overly aggressive in adjusting the `customMarginTop` of the neighbor below the new Free Text item. My primary focus is on removing a double count of height which is pushing the item down more than required. The code is compensating when it shouldn't.\n\n\n\n**Clarifying Margin Compensation**\n\nI now understand that when a Free Text item enters a category, the key is margin compensation, not height. The loss of margin on the item being added causes an unwanted upward shift of the neighbor. Therefore, I need to add that margin to the neighbor, to maintain its visual position, not the height. I'm reviewing the exit behavior, now.\n\n\n**Evaluating Category Shifts**\n\nI am now focused on the effects of moving an item down, to a new \"ghost\" category when the current category is at its bottom. The code creates a new ghost category (ft_zone_...) to house the item, and now I'm evaluating its interaction with the item below, FT2. I'm focusing on preserving FT2's visual position. The height of the original category decreases and the ghost category appears. Total height should be roughly conserved, excluding headers. The key is in maintaining the visual continuity for FT2.\n\n\n**Analyzing Upward Category Merging**\n\nI'm now zeroing in on cases where a Free Text item attempts to *merge upwards* into a category, specifically through up arrow navigation from within the category. I am evaluating whether `compensatableNeighbor` is correctly handling this category exit. The goal is to determine if the item's position, and the position of neighbors, are preserved during this transition, particularly if the item merges *up* into the category above.\n\n\n**Refining Position Adjustments**\n\nI'm now clarifying the intended absolute positioning of Free Text items during category entry. The goal is to correct any unintended shifts, focusing on the neighbor (FT2) below when a Free Text item (FT1) enters a category. My analysis indicates that the code should compensate for margin changes, to maintain FT2's visual position. No height compensation is needed. I'm focusing on the category transition to ensure no position shifts.\n\n\n\n**Evaluating Category Exits**\n\nI'm now focusing on Free Text item (FT1) behavior when it *exits* a category, particularly through upward movement. My current hypothesis centers on whether it creates a new zone and its interaction with the neighbor (FT2) previously below FT1 within that category. I'm investigating how this \"exit\" impacts FT2's absolute position and if I have correctly modeled the new zone.\n\n\n\n**Halting Height Compensation**\n\nI'm now investigating a category shift when the current category is at its bottom and an item is moved down. The \"descending\" behavior I'm seeing leads me to believe I am inappropriately altering the neighbor's margin. The visual height of the original category has decreased and the ghost category appears. I'm focusing on ensuring visual continuity for the neighbor item, while respecting the rule that position should only be changed if the user clicks.\n\n\n\n**Verifying Boundary Behavior**\n\nI'm now focusing on the \"descending\" behavior when a Free Text item leaves a category, particularly the \"Bottom Exit\" scenario. My current thought is that it doesn't have an explicit compensation logic for neighbors in the `down` block within the provided code snippet. Then, I am going to see if `Try B: ENTER CATEGORY` and `Try C: HEADER EXIT` are relevant inside `direction === 'up'`. I am still looking for the down logic.\n\n\n\n**Confirming Bottom Exit Logic**\n\nI've been analyzing \"Bottom Exit\" scenarios, and I've found the issue. The \"descending\" problem during bottom exit seems to stem from a lack of explicit compensation for neighbors in the `down` block within the code. I am thinking I need to investigate DOM flow. I am focusing on what might happen during \"Bottom Exit\" scenarios when the logic updates the products, but doesn't handle the neighbor's position directly. I also need to verify up arrow navigation now.\n\n\n\n**Correcting Height Compensation**\n\nI'm now zeroing in on removing `heightComp` from the `up` direction, specifically within the \"HEADER EXIT\" and \"ENTER CATEGORY\" scenarios. My analysis shows these cases, especially with Free Text items, are leading to unintended vertical shifts. I'm focusing on ensuring FT2 stays put with the margin correction. It seems the neighbor should only move if the user clicks.\n\n\n**Refining Category Calculations**\n\nI'm now investigating a discrepancy. I've realized the `heightComp` is the issue, and removing it makes FT2, the neighbor, remain stationary. The intent of the original logic seems to be to keep the visual position of items constant, not to keep them aligned to the bottom. I'm focusing on why items can have a visual descending shift. I suspect I need to update the margin and keep FT2 stationary.\n\n\n\n**Validating Neighbor Adjustments**\n\nI'm now focusing on validating the height and margin compensation adjustments for FT2, the neighbor, during category transitions involving FT1. My analysis indicates that the `heightComp` calculation is unnecessary for FT2's visual stability, and may be driving the descending shift. Removing `heightComp` from `Try B` and `Try C` is the key. I am verifying those adjustments.\n\n\n**Verifying Header Exit Logic**\n\nI'm now investigating the potential implications of removing height compensation in the \"HEADER EXIT\" scenario. The function is now potentially unused in these blocks, so I need to check its other usages. Specifically, I'm focusing on the interaction when an item moves up past a header. The visual scenario is: Category A, Header B, Item 1, Neighbor. Item 1 merges into Category A, pushing Header B and the Neighbor down. I'm now exploring if the Neighbor truly needs extra compensation in this case, and if its visual position will change.\n\n\n\n**Evaluating Neighbor Movement**\n\nI'm now clarifying the neighbor's motion when an item exits its previous position and jumps above a header. The neighbor will physically shift down, not up, because the header is moving. To keep the neighbor's visual position, the appropriate height compensation is necessary when the item moves above a header. However, margin compensation is not needed.\n\n\n**Confirming Category Interaction**\n\nI'm now confirming the category interactions to address unintended movements. The key lies in preserving visual position, regardless of the flow. Header B moves down as expected. Neighbor moves up relative to it by H1+M1. The neighbor's total absolute position changes by M1, needing margin compensation. Eliminating `heightComp` fixes the descending issue, as it was adding extra height. I am removing `heightComp` from updates in the described scenarios.\n\n\n**Verifying Upward Movement**\n\nI'm now clarifying the behavior when a Free Text item leaves a category via upward movement.  My code creates a ghost category, so I'm evaluating the impact on any neighbor items below the original category.  I'm focused on the \"Bottom Exit\" scenario. It seems a neighbor FT2 pushes FT1 down with `Collision Logic`, which is a swap. This behavior appears correct.\n\n\n**Clarifying Downward Transitions**\n\nI'm now zeroing in on the \"Bottom Exit\" scenario when moving a Free Text item downwards.  It looks like there's a misunderstanding of how the items should shift to a new ghost category. I'm focused on the edge case where the neighbor item (FT2) is also a Free Text item. The logic should be corrected, focusing on the neighbor (FT2) and FT1, during this transition.\n\n\n**Analyzing Category Flow**\n\nI'm now investigating a \"descending\" behavior when a Free Text item (FT1) transitions to join the zone of the item below (FT2). I see that FT2's margin is not touched, and that makes FT2 shift downwards. So, I will correct that shift by reducing FT2's margin by FT1's height, keeping FT2 stationary. The logic needs to correct FT2's visual position.\n\n\n**Confirming Height Conservation**\n\nI'm now confirming height conservation in category transitions, specifically when a Free Text item (FT1) exits a category via upward or downward movement. The key is now verifying the \"Bottom Exit\" scenario. The code should maintain the neighbor's visual position, FT2, by adjusting its margin. My analysis leads me to remove unnecessary height compensation, so the neighbor, FT2, stays stationary.\n\n\n**Confirming Visual Stability**\n\nI'm now zeroing in on confirming visual stability in \"Bottom Exit\" scenarios when FT1 moves down into FT2's zone, so I removed the heightComp and am focusing on correcting FT2's margin. This involves making adjustments to FT2's margin to counteract unintended visual shifts, so that FT2's visual position stays put.\n\n\n**Verifying Function Removal**\n\nNow, I'm verifying the full impact of removing the `getHeightCompensation` helper function, checking for any unintended consequences or unused calls. I've re-examined the code, confirming it is not used after removing the height compensation in all the places it was called. The code remains clean.\n\n\n**Validating Margin Adjustments**\n\nI'm now implementing the margin adjustments, removing the now-unnecessary `heightComp` from several scenarios. Specifically, I'm focusing on \"Bottom Exit,\" \"Header Exit,\" and \"Enter Category\" scenarios. By removing it, I expect the target neighbor to maintain its visual position, resolving the \"descending\" issue. I am verifying this will avoid unintended position shifts.\n\n\n**Finalizing Visual Stability**\n\nI'm now fully confident. The analysis and testing have clarified the need for margin compensation, not height. The `getHeightCompensation` function is removed, which now makes FT2 stationary. My test setup ensures visual stability during category transitions. This keeps the items in the same position unless the user has requested a change. The update is ready.\n\n\n**Refining Bottom Exit Logic**\n\nI'm now zeroing in on the interaction between FT1 exiting a category and its neighbor, FT2, below, focusing on the \"Bottom Exit\" scenario. The recent modifications involve removing the unnecessary height compensation to maintain FT2's visual position. I'm carefully evaluating all related edge cases to ensure the flow is correct.\n\n\n",
      "cMarkNode": {}
    },
    "createdTimestamp": {
      "seconds": 1769269317,
      "nanos": 404000000
    },
    "doneTimestamp": {
      "seconds": 1769269485,
      "nanos": 382000000
    }
  },
  {
    "id": 19,
    "author": "model",
    "payload": {
      "type": "text",
      "text": "To ensure the Free Text item positions remain absolute and do not shift (descend) when a neighbor enters or leaves a category, I will update `hooks/interactions/useKeyboardMovement.ts`.\n\nThe issue was caused by an over-compensation logic where the estimated height of the moving item (`heightComp`) was being added to the neighbor's margin. Since the HTML flow naturally accounts for the item's height when it moves into a parent container (causing the container to expand), adding the height to the margin was redundant and caused the neighbor to move down (descend).\n\nI will remove the `getHeightCompensation` function and its usage, ensuring we only transfer the `currentMargin` to the neighbor to preserve the visual gap.\n\n",
      "cMarkNode": {}
    },
    "createdTimestamp": {
      "seconds": 1769269485,
      "nanos": 383000000
    },
    "doneTimestamp": {
      "seconds": 1769269485,
      "nanos": 586000000
    }
  },
  {
    "id": 20,
    "author": "model",
    "payload": {
      "type": "generationTable",
      "entries": [
        {
          "path": "hooks/interactions/useKeyboardMovement.ts",
          "generationType": 1,
          "isDone": true,
          "description": "Remove height compensation from Free Text movement logic to prevent neighbors from shifting (descending) when an item enters/leaves a category.",
          "diffs": [
            {
              "target": "",
              "replacement": "import React, { useRef, useMemo } from 'react';\nimport { Product } from '../../types';\nimport { InteractionProps, DraftItem, NUDGE_STEP, FREE_TEXT_PREFIX, STANDARD_GAP, A4_HEIGHT_PX } from './types';\n\nexport const useKeyboardMovement = (\n    props: InteractionProps,\n    sortedCategories: string[],\n    groupedProducts: Record<string, Product[]>,\n    draftItem: DraftItem | null,\n    setDraftItem: (item: DraftItem | null) => void\n) => {\n    const { \n        products, scale,\n        onMoveCategory, onMoveProduct, onUpdateProduct, onUpdateProducts,\n        onCommitProductOrder, onStyleUpdate, onAddProduct, onAddCategory, onToggleProductVisibility, onDeleteProduct\n    } = props;\n\n    const draftInputRef = useRef<HTMLDivElement>(null);\n\n    // 3. Flattened Visual List (For Arrow Navigation)\n    const visualList = useMemo(() => {\n        const list: { type: 'header' | 'product', id: string, category: string, product?: Product, globalIndex: number }[] = [];\n        let gIdx = 0;\n        sortedCategories.forEach(cat => {\n            list.push({type: 'header',id: cat,category: cat,globalIndex: gIdx++});\n            const prods = groupedProducts[cat] || [];\n            prods.forEach(p => {\n                list.push({ type: 'product', id: p.id, category: cat, product: p, globalIndex: gIdx++ });\n            });\n        });\n        return list;\n    }, [sortedCategories, groupedProducts]);\n\n    const handleGlobalMove = (e: React.MouseEvent, type: 'category' | 'product', id: string, catName: string, direction: 'up' | 'down') => {\n        e.stopPropagation();\n        \n        // Standard Category Move\n        if (type === 'category') {\n            onMoveCategory?.(id, direction);\n            return;\n        }\n        \n        const product = products.find(p => p.id === id);\n        if (!product) return;\n\n        if (!product.isFreeText) {\n            onMoveProduct?.(id, catName, direction);\n            return;\n        }\n        \n        // --- UNIFIED FREE TEXT MOTION LOGIC ---\n        const currentIndex = visualList.findIndex(item => item.id === id);\n        if (currentIndex === -1) return;\n        \n        const neighbor = direction === 'up' ? visualList[currentIndex - 1] : visualList[currentIndex + 1];\n        \n        // --- MOVE UP (FAIL-SAFE FLOW) ---\n        if (direction === 'up') {\n            // 1. ROBUST DATA FETCHING\n            const currentMargin = Number(product.customMarginTop || 0);\n            const neighborAbove = visualList[currentIndex - 1]; \n            const neighborBelow = visualList[currentIndex + 1];\n\n            // Helper to get the actual item below to compensate (skipping its header if needed)\n            const getCompensatableNeighborBelow = () => {\n                let target = neighborBelow;\n                if (target && target.type === 'header') {\n                    // If neighbor below is a header, look at the first product inside it\n                    target = visualList[currentIndex + 2];\n                }\n                return (target && target.type === 'product' && target.product) ? target : null;\n            };\n\n            // 2. INTERACTION PHASE (Only if touching neighbor above)\n            if (neighborAbove && currentMargin <= STANDARD_GAP) {\n                \n                // Try A: SWAP (Free Text or Same Category)\n                if (neighborAbove.type === 'product' && neighborAbove.product &&\n                    (neighborAbove.product.isFreeText || neighborAbove.product.category === product.category)) {\n                    \n                    if (onUpdateProducts && onCommitProductOrder) {\n                        const neighborMargin = Number(neighborAbove.product.customMarginTop ?? 0);\n                        const updates: { id: string, field: keyof Product, value: any }[] = [\n                            { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                            { id: neighborAbove.id, field: 'customMarginTop' as keyof Product, value: currentMargin },\n                        ];\n                        if (product.category !== neighborAbove.product.category) {\n                            updates.push({ id: product.id, field: 'category' as keyof Product, value: neighborAbove.product.category });\n                            updates.push({ id: neighborAbove.id, field: 'category' as keyof Product, value: product.category });\n                        }\n                        onUpdateProducts(updates);\n\n                        if (product.category === neighborAbove.product.category) {\n                            const currentOrder = groupedProducts[product.category].map(p => p.id);\n                            const idx1 = currentOrder.indexOf(id);\n                            const idx2 = currentOrder.indexOf(neighborAbove.id);\n                            if (idx1 !== -1 && idx2 !== -1) {\n                                const newOrder = [...currentOrder];\n                                [newOrder[idx1], newOrder[idx2]] = [newOrder[idx2], newOrder[idx1]];\n                                onCommitProductOrder(product.category, newOrder);\n                            }\n                        }\n                    }\n                    return; // Success\n                }\n\n                // Try B: ENTER CATEGORY (Bottom-Up Entry - Direct Neighbor Product)\n                if (neighborAbove.type === 'product' && neighborAbove.product && \n                    !neighborAbove.product.category.startsWith(FREE_TEXT_PREFIX)) {\n                        \n                        const targetCat = neighborAbove.product.category;\n                        if (onUpdateProducts && onCommitProductOrder) {\n                            const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                            const neighborIndex = currentOrder.indexOf(neighborAbove.id);\n                            const newOrder = [...currentOrder];\n                            \n                            // Append after neighbor (index + 1)\n                            if (neighborIndex !== -1) newOrder.splice(neighborIndex + 1, 0, product.id);\n                            else newOrder.push(product.id);\n\n                            const updates: { id: string, field: keyof Product, value: any }[] = [\n                                { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                                { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                            ];\n\n                            // COMPENSATION FIX: Pass ONLY margin to neighbor below (Height is conserved by flow)\n                            const compensatableNeighbor = getCompensatableNeighborBelow();\n                            if (compensatableNeighbor && compensatableNeighbor.product) {\n                                const nextMargin = Number(compensatableNeighbor.product.customMarginTop || 0);\n                                updates.push({\n                                    id: compensatableNeighbor.id,\n                                    field: 'customMarginTop',\n                                    value: nextMargin + currentMargin\n                                });\n                            }\n\n                            onUpdateProducts(updates);\n                            onCommitProductOrder(targetCat, newOrder);\n                        }\n                        return; // Success\n                }\n                \n                // Try C: HEADER EXIT\n                if (neighborAbove.type === 'header') {\n                        // MERGE INTO CATEGORY ABOVE logic\n                        const itemAboveHeader = visualList[currentIndex - 2];\n\n                        // Common Logic for merging up\n                        const performMergeUp = (targetCat: string) => {\n                             if (onUpdateProducts && onCommitProductOrder) {\n                                 const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                                 const newOrder = currentOrder.includes(product.id) ? currentOrder : [...currentOrder, product.id];\n                                 \n                                 const updates: { id: string, field: keyof Product, value: any }[] = [\n                                     { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                                     { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                                 ];\n\n                                 // COMPENSATION FIX: Pass ONLY margin to neighbor below (Height is conserved by flow)\n                                 const compensatableNeighbor = getCompensatableNeighborBelow();\n                                 if (compensatableNeighbor && compensatableNeighbor.product) {\n                                     const nextMargin = Number(compensatableNeighbor.product.customMarginTop || 0);\n                                     updates.push({\n                                         id: compensatableNeighbor.id,\n                                         field: 'customMarginTop',\n                                         value: nextMargin + currentMargin\n                                     });\n                                 }\n\n                                 onUpdateProducts(updates);\n                                 onCommitProductOrder(targetCat, newOrder);\n                            }\n                        };\n\n                        // Case 1: Above header is a product in a regular category\n                        if (itemAboveHeader && itemAboveHeader.type === 'product' && \n                            !itemAboveHeader.category.startsWith(FREE_TEXT_PREFIX)) {\n                                performMergeUp(itemAboveHeader.category);\n                                return;\n                        }\n\n                        // Case 2: Above header is a header of a regular category (empty)\n                        if (itemAboveHeader && itemAboveHeader.type === 'header' && \n                            !itemAboveHeader.category.startsWith(FREE_TEXT_PREFIX)) {\n                                performMergeUp(itemAboveHeader.category);\n                                return;\n                        }\n\n                        // Fallback: Create new ghost category\n                        if (onStyleUpdate && onUpdateProduct) {\n                            const newGhostCat = `${FREE_TEXT_PREFIX}${crypto.randomUUID()}`;\n                            onStyleUpdate(prev => {\n                                const cats = [...(prev.customCategoryOrder || sortedCategories)];\n                                const idx = cats.indexOf(neighborAbove.category);\n                                if (idx !== -1) cats.splice(idx, 0, newGhostCat);\n                                return { ...prev, customCategoryOrder: cats, name: 'Custom' };\n                            });\n                            onUpdateProduct(id, 'category', newGhostCat);\n                        }\n                        return; // Success\n                }\n\n                // Try D: FORCE SWAP (Fallback for Blocking)\n                // If we reached here, we are touching a product but couldn't enter. \n                // We MUST jump over it to prevent getting stuck.\n                if (neighborAbove.type === 'product' && neighborAbove.product) {\n                        // Treat as swap even if different category (simplistic fallback)\n                        if (onUpdateProducts) {\n                            const neighborMargin = Number(neighborAbove.product.customMarginTop ?? 0);\n                            onUpdateProducts([\n                            { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                            { id: neighborAbove.id, field: 'customMarginTop' as keyof Product, value: currentMargin },\n                            // Swap categories\n                            { id: product.id, field: 'category' as keyof Product, value: neighborAbove.product.category },\n                            { id: neighborAbove.id, field: 'category' as keyof Product, value: product.category }\n                        ]);\n                        }\n                        return;\n                }\n            }\n\n            // 3. MOVEMENT PHASE (Nudge Up)\n            // If we didn't interact (or had space), we move up.\n            \n            const newMyMargin = Math.max(0, currentMargin - NUDGE_STEP);\n            const updates: { id: string, field: keyof Product, value: any }[] = [\n                { id: product.id, field: 'customMarginTop', value: newMyMargin }\n            ];\n\n            // B. COMPENSATE NEIGHBOR BELOW (The Snowplow Fix)\n            // CRITICAL: This MUST run if neighborBelow exists, regardless of anything else.\n            \n            let neighborToCompensate = neighborBelow;\n\n            // Fix for Free Text / Cross-Category: \n            // If the immediate neighbor is a header, we check the item AFTER the header.\n            // If that item is a product, we apply compensation to it instead.\n            if (neighborToCompensate && neighborToCompensate.type === 'header') {\n                const nextVisual = visualList[currentIndex + 2];\n                if (nextVisual && nextVisual.type === 'product') {\n                    neighborToCompensate = nextVisual;\n                }\n            }\n\n            if (neighborToCompensate && neighborToCompensate.type === 'product' && neighborToCompensate.product) {\n                const nextMarginRaw = neighborToCompensate.product.customMarginTop;\n                // Force Number() to prevent NaN\n                const nextMarginSafe = Number(nextMarginRaw ?? 0); \n                \n                // We add EXACTLY the same amount we subtracted from the item above (Conservation of Space)\n                updates.push({ \n                    id: neighborToCompensate.id, \n                    field: 'customMarginTop', \n                    value: nextMarginSafe + NUDGE_STEP \n                });\n            }\n\n            onUpdateProducts?.(updates);\n        }\n\n        // --- MOVE DOWN ---\n        if (direction === 'down') {\n            // Bottom Exit (Leaving Category Downwards)\n            if ((!neighbor || neighbor.type === 'header') && !catName.startsWith(FREE_TEXT_PREFIX)) {\n                if (onStyleUpdate && onUpdateProducts) {\n                    const newGhostCat = `${FREE_TEXT_PREFIX}${crypto.randomUUID()}`;\n                    onStyleUpdate(prev => {\n                        const cats = [...(prev.customCategoryOrder || sortedCategories)];\n                        const idx = cats.indexOf(catName);\n                        if (idx !== -1) cats.splice(idx + 1, 0, newGhostCat); // Insert AFTER\n                        else cats.push(newGhostCat);\n                        return { ...prev, customCategoryOrder: cats, name: 'Custom' };\n                    });\n                    onUpdateProducts([\n                        { id: product.id, field: 'category' as keyof Product, value: newGhostCat },\n                        { id: product.id, field: 'customMarginTop' as keyof Product, value: STANDARD_GAP }\n                    ]);\n                }\n                return;\n            }\n\n            // Allow moving down (increasing margin) even if no neighbor in a ghost zone\n            if (!neighbor && catName.startsWith(FREE_TEXT_PREFIX)) {\n                    const myMargin = product.customMarginTop || 0;\n                    onUpdateProduct?.(id, 'customMarginTop', myMargin + NUDGE_STEP);\n                    return;\n            }\n\n            if (!neighbor) return;\n\n            // 1. Physics Check (Nudge Down)\n            if (neighbor.type === 'product' && neighbor.product) {\n                const neighborMargin = neighbor.product.customMarginTop || 0;\n\n                // If neighbor has margin, consume it (Zero-Sum)\n                if (neighborMargin > STANDARD_GAP) {\n                    const myMargin = product.customMarginTop || 0;\n                    onUpdateProducts?.([\n                        { id: product.id, field: 'customMarginTop' as keyof Product, value: myMargin + NUDGE_STEP },\n                        { id: neighbor.id, field: 'customMarginTop' as keyof Product, value: neighborMargin - NUDGE_STEP }\n                    ]);\n                    return;\n                } else if (catName.startsWith(FREE_TEXT_PREFIX)) {\n                        // Fallthrough to collision\n                }\n            }\n\n            // --- COLLISION LOGIC ---\n\n            // FT vs FT Collision (Down)\n            if (neighbor.type === 'product' && neighbor.product?.isFreeText) {\n                    if (onUpdateProducts) {\n                        const myMargin = product.customMarginTop || 0;\n                        const neighborMargin = neighbor.product.customMarginTop || 0;\n                        onUpdateProducts([\n                            { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                            { id: neighbor.id, field: 'customMarginTop' as keyof Product, value: myMargin },\n                            { id: product.id, field: 'category' as keyof Product, value: neighbor.category },\n                            { id: neighbor.id, field: 'category' as keyof Product, value: catName }\n                        ]);\n                        if (neighbor.category === catName && onCommitProductOrder) {\n                        const currentOrder = groupedProducts[catName].map(p => p.id);\n                        const idx1 = currentOrder.indexOf(id);\n                        const idx2 = currentOrder.indexOf(neighbor.id);\n                        const newOrder = [...currentOrder];\n                        [newOrder[idx1], newOrder[idx2]] = [newOrder[idx2], newOrder[idx1]];\n                        onCommitProductOrder(catName, newOrder);\n                    }\n                    }\n                    return;\n            }\n            \n            // Case A: Neighbor is Header (Next Category)\n            if (neighbor.type === 'header') {\n                // Enter Category (Top)\n                if (onUpdateProducts && onCommitProductOrder) {\n                    const targetCat = neighbor.id;\n                    onUpdateProducts([\n                        { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                        { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                    ]);\n                    const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                    onCommitProductOrder(targetCat, [product.id, ...currentOrder]);\n                }\n                return;\n            }\n\n            // Case B: Neighbor is Product\n            if (neighbor.type === 'product' && neighbor.product) {\n                    if (neighbor.category === catName) {\n                        if (onCommitProductOrder && onUpdateProducts) {\n                            const currentOrder = groupedProducts[catName].map(p => p.id);\n                            const idx1 = currentOrder.indexOf(id);\n                            const idx2 = currentOrder.indexOf(neighbor.id);\n                            const newOrder = [...currentOrder];\n                            [newOrder[idx1], newOrder[idx2]] = [newOrder[idx2], newOrder[idx1]];\n\n                            const myMargin = product.customMarginTop || 0;\n                            const neighborMargin = neighbor.product.customMarginTop || 0;\n                            onUpdateProducts([\n                                { id: product.id, field: 'customMarginTop' as keyof Product, value: neighborMargin },\n                                { id: neighbor.id, field: 'customMarginTop' as keyof Product, value: myMargin }\n                            ]);\n                            onCommitProductOrder(catName, newOrder);\n                        }\n                        return;\n                    }\n                    \n                    // Subcase B2: Different Category\n                    if (neighbor.category !== catName) {\n                        const targetCat = neighbor.category;\n                        if (onUpdateProducts && onCommitProductOrder) {\n                            onUpdateProducts([\n                                { id: product.id, field: 'category' as keyof Product, value: targetCat },\n                                { id: product.id, field: 'customMarginTop' as keyof Product, value: 0 }\n                            ]);\n                            // Insert Before Neighbor\n                            const currentOrder = groupedProducts[targetCat]?.map(p => p.id) || [];\n                            const nIdx = currentOrder.indexOf(neighbor.id);\n                            const newOrder = [...currentOrder];\n                            if (nIdx !== -1) newOrder.splice(nIdx, 0, product.id);\n                            else newOrder.unshift(product.id);\n                            \n                            onCommitProductOrder(targetCat, newOrder);\n                        }\n                        return;\n                    }\n            }\n        }\n    };\n\n    const handlePageDoubleClick = (e: React.MouseEvent, pageIndex: number, handleSelection: any, setSelectedPageIndex: any) => {\n        e.stopPropagation();\n        handleSelection(null, null);\n        \n        const pageEl = e.currentTarget as HTMLElement;\n        const pageRect = pageEl.getBoundingClientRect();\n        const currentScale = (typeof scale === 'number' && scale > 0) ? scale : 1;\n        const clickY = (e.clientY - pageRect.top) / currentScale;\n        \n        let floorId: string | null = null;\n        let floorBottom = 0; \n        let ceilingId: string | null = null;\n        let ceilingTop = A4_HEIGHT_PX; \n        \n        let minDistAbove = Infinity;\n        let minDistBelow = Infinity;\n\n        const allElements = pageEl.querySelectorAll('[id^=\"product-container-\"], [id^=\"category-header-\"], [data-block-id=\"main-header\"]');\n        \n        allElements.forEach(el => {\n            const rect = el.getBoundingClientRect();\n            const itemTop = (rect.top - pageRect.top) / currentScale;\n            const itemHeight = rect.height / currentScale;\n            const itemBottom = itemTop + itemHeight;\n            \n            let id = el.getAttribute('data-category-id') || el.getAttribute('data-block-id');\n            if (!id && el.id.startsWith('product-container-')) id = el.id.replace('product-container-', '');\n            if (!id) return;\n            \n            if (itemBottom < clickY) {\n                const dist = clickY - itemBottom;\n                if (dist < minDistAbove) {\n                    minDistAbove = dist;\n                    floorId = id;\n                    floorBottom = itemBottom;\n                }\n            } else if (itemTop > clickY) {\n                const dist = itemTop - clickY;\n                if (dist < minDistBelow) {\n                    minDistBelow = dist;\n                    ceilingId = id;\n                    ceilingTop = itemTop;\n                }\n            }\n        });\n        \n        setDraftItem({\n            pageIndex,\n            top: clickY,\n            floorId,\n            floorBottom,\n            ceilingId,\n            ceilingTop\n        });\n\n        setTimeout(() => {\n            if (draftInputRef.current) draftInputRef.current.focus();\n        }, 50);\n    };\n\n    const handleDraftCommit = () => {\n        if (!draftItem || !onAddProduct || !onStyleUpdate || !draftInputRef.current) return;\n        const text = draftInputRef.current.innerText || \"New Text\";\n        const newId = crypto.randomUUID();\n        const ghostCategoryName = `${FREE_TEXT_PREFIX}${newId}`;\n        \n        const newItemMargin = Math.max(0, draftItem.top - draftItem.floorBottom);\n        \n        if (draftItem.ceilingId && onUpdateProduct) {\n            const currentGap = draftItem.ceilingTop - draftItem.floorBottom;\n            const ESTIMATED_HEIGHT = 40; \n            const newCeilingMargin = Math.max(0, currentGap - newItemMargin - ESTIMATED_HEIGHT);\n            \n            const isCeilingProduct = products.find(p => p.id === draftItem.ceilingId);\n            if (isCeilingProduct) {\n                onUpdateProduct(draftItem.ceilingId, 'customMarginTop', newCeilingMargin);\n            }\n        }\n        \n        onStyleUpdate(prev => {\n            const currentOrder = prev.customCategoryOrder && prev.customCategoryOrder.length > 0 \n                ? [...prev.customCategoryOrder] \n                : [...sortedCategories];\n            \n            sortedCategories.forEach(c => { if (!currentOrder.includes(c)) currentOrder.push(c); });\n\n            let insertIndex = currentOrder.length;\n            \n            if (draftItem.ceilingId) {\n               const cProd = products.find(p => p.id === draftItem.ceilingId);\n               const cCat = cProd ? cProd.category : (currentOrder.includes(draftItem.ceilingId!) ? draftItem.ceilingId : null);\n               if (cCat) {\n                   const idx = currentOrder.indexOf(cCat);\n                   if (idx !== -1) insertIndex = idx;\n               }\n            } else if (draftItem.floorId) {\n               const fProd = products.find(p => p.id === draftItem.floorId);\n               const fCat = fProd ? fProd.category : (currentOrder.includes(draftItem.floorId!) ? draftItem.floorId : null);\n               if (fCat) {\n                   const idx = currentOrder.indexOf(fCat);\n                   if (idx !== -1) insertIndex = idx + 1;\n               }\n            }\n\n            currentOrder.splice(insertIndex, 0, ghostCategoryName);\n\n            const newProdOrder = { ...(prev.customProductOrder || {}) };\n            newProdOrder[ghostCategoryName] = [newId];\n\n            return {\n                ...prev,\n                customCategoryOrder: currentOrder,\n                customProductOrder: newProdOrder,\n                name: 'Custom'\n            };\n        });\n\n        onAddProduct(ghostCategoryName, undefined, true, newId, { customMarginTop: newItemMargin, name: text });\n        setDraftItem(null);\n    };\n\n    const handleRemove = (e: React.MouseEvent, id: string, type: 'product' | 'category') => {\n        e.stopPropagation();\n        e.preventDefault();\n        \n        if (type === 'product') {\n             const product = products.find(p => p.id === id);\n             if (product?.isFreeText) {\n                 // Free Text: PERMANENT DELETE\n                 onDeleteProduct?.(id);\n             } else {\n                 // Regular Product: HIDE (toggle off)\n                 onToggleProductVisibility?.(id, false);\n             }\n        } else {\n            // Category: HIDE ALL PRODUCTS inside (toggle off)\n            const catProducts = groupedProducts[id] || [];\n            if (onStyleUpdate) {\n                 onStyleUpdate(prev => {\n                     const currentHidden = new Set(prev.hiddenProductIds || []);\n                     catProducts.forEach(p => currentHidden.add(p.id));\n                     return { ...prev, hiddenProductIds: Array.from(currentHidden), name: 'Custom' };\n                 });\n            }\n        }\n    };\n    \n    // Wrapper for add functionality if handlers needs to expose it for MenuItem convenience\n    const handleAddClick = (e: React.MouseEvent, category: string, isCategoryAdd: boolean, position: 'before' | 'after') => {\n        if (isCategoryAdd) {\n            onAddCategory?.(category, position);\n        } else {\n            onAddProduct?.(category, undefined, false, undefined, undefined, { index: 0 }); \n        }\n    };\n\n    return {\n        visualList,\n        draftInputRef,\n        handleGlobalMove,\n        handlePageDoubleClick,\n        handleDraftCommit,\n        handleRemove,\n        handleAddClick\n    };\n};"
            }
          ]
        }
      ]
    },
    "createdTimestamp": {
      "seconds": 1769269487,
      "nanos": 26000000
    },
    "doneTimestamp": {
      "seconds": 1769269538,
      "nanos": 121000000
    }
  }
]